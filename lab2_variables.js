// Laboratory Activity 2: TypeScript Variable Types//
// Section 1: Primitive Types
var age = 19;
console.log(age);
var isEnrolled = true;
console.log(isEnrolled);
var studentId = 20232386;
console.log(studentId);
var courseName = "Application Development 1";
console.log(courseName);
// Section 2: Null and Undefined
/*Declare a variable named `middleName` that can be either a `string` or `null`. Assign it an appropriate value.
Declare a variable named `graduationYear` of type `number` or `undefined`. Leave it undefined initially.
Declare a variable named `emailAddress` that can accept `string` or `null`. Assign it your email address.
For union types (variables that can hold multiple types), use the pipe symbol `|`
Test your variables by logging them to the console*/
var middleName = "Celestino";
console.log(middleName);
var graduationYear = undefined;
console.log(graduationYear);
var emailAddress = "20232386@studentId.ubaguio.edu";
console.log(emailAddress);
